import { Documents } from '@/widgets/Documents';
import { Tabs } from '@/widgets/Tabs';

export const DocumentsPage = () => {
    return (
        <>
            <Tabs />
            <Documents />
        </>
    );
};
