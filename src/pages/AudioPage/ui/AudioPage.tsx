import { Audio } from '@/widgets/Audio/';
import { Tabs } from '@/widgets/Tabs';

const AudioPage = () => {
    return (
        <>
            <Tabs />
            <Audio audioItems={[]} />
        </>
    );
};

export default AudioPage;
