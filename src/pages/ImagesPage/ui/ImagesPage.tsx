import { Tabs } from '@/widgets/Tabs';
import {Images} from "@/widgets/Images";

export const ImagesPage = () => {
    return (
        <>
            <Tabs />
            <Images images={[]} />
        </>
    );
};
